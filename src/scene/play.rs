use crate::scene::*;
// use crate::chart::*;

#[derive(Debug)]
pub struct Play {}

impl Scene for Play {
    fn init(&self) {
        // Initialize the play scene
    }

    fn update(&self) -> State {
        // Update the play scene
        State::Continue
    }

    fn render(&self) {
        // Render the play scene
    }

    fn quit(&self) {
        // Quit the play scene
    }
}
